name: Final-Lab2-Check
on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Report
        run: |
          echo "Лабораторна 2: Перевірка завершена"
          echo "Студент: Станіслав Бандура"
